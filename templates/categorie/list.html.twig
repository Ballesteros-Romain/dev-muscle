{% extends "base.html.twig" %}
{% block title %}{% endblock %}
{% block body %}
    <main class="container">
        <section class="row">
            <div class="col-12">
            <h1>Liste des produits de la categorie {{category.name}}</h1>
            </div>
            {% for product in products %}
                <div class="col-3">
                    <article class="card h-100">
                        <img src="{{asset('assets/upload/products/mini/300x-300-' ~ product.images[0].name)}}" class="card-img-top" alt="...">
                        <div class="card-body">
                             <h2 class="card-title">{{product.name}}</h2>
                             <div class="card-text">{{product.descripton}}</div>
                             <a href="{{path('product_details', {"slug": product.slug})}}" class="btn btn-primary">Voir les détails</a>
                        </div>
                    </article>
                </div>
            {% endfor %}
        </section>
    </main>
{% endblock %}